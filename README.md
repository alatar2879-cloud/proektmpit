python.exe -m pip install --upgrade pip
 pip install networkx
  pip install graphviz
   pip install source
    pip install nx
    pip install Image 
Шаблон промта для ChatGpt
Создай граф из 50 ученых и исследователей в области ИИ, с указанием их работ и ссылок на их Google Scholar, и рассортируй их по субграфам(Областям исследования). Графы должны соответствовать формату: "<id>" [label="Человек", work="Work1", href="https://..."];

    
